# 🚀 Запуск проекта UniChance

## Предварительные требования

```bash
✅ Node.js (v18+)
✅ Docker Desktop
✅ Go (optional, для разработки backend)
```

## 1️⃣ Запуск БД (PostgreSQL через Docker)

Поднимите базу данных:

```bash
cd c:\Users\User\unichance
docker-compose up -d
```

⏳ **Первый раз может занять 2-3 минуты!** Дождитесь, пока контейнер полностью запустится.

Проверьте статус:
```bash
docker ps
```

Должен быть запущен контейнер `unichance_db` на порту 5432.

## 2️⃣ Запуск Backend (Go API)

```bash
cd c:\Users\User\unichance\backend-go
.\api.exe
```

Backend запустится на **http://localhost:8080**

Проверьте:
```bash
# В другом терминале
curl http://localhost:8080/health
# Должен вернуть: ok
```

## 3️⃣ Запуск Frontend (React + Vite)

```bash
# В третьем терминале
cd c:\Users\User\unichance
npm run dev
```

Frontend запустится на **http://localhost:5173**

Откройте браузер:
```
http://localhost:5173/search
```

## Полный стек:

| Компонент | Порт | URL |
|---|---|---|
| **Frontend (Vite/React)** | 5173 | http://localhost:5173 |
| **Backend (Go API)** | 8080 | http://localhost:8080 |
| **Database (PostgreSQL)** | 5432 | localhost:5432 |

## ✅ Проверка подключения

1. **Фронт подключён к бэку:**
   - Откройте DevTools (F12)
   - Перейдите на страницу поиска
   - Выполните поиск
   - В Network tab должны видны запросы к `/programs`

2. **Бэк подключён к БД:**
   - Если видны результаты в поиске - вся цепь работает!
   - Если есть ошибка "failed to connect to database" - БД ещё загружается

## 🧹 Остановка всего

```bash
# Остановить Docker
docker-compose down

# Или убить конкретный контейнер
docker stop unichance_db
```

## 🐛 Troubleshooting

**Docker контейнер медленно загружается:**
- Первый запуск: ждите 3-5 минут (скачивает image)
- Последующие: несколько секунд

**Ошибка подключения к БД:**
```
failed to connect to `user=unichance database=unichance`
```
→ Контейнер ещё загружается, подождите, потом обновите страницу

**Port уже занят:**
```bash
# Найдите что занимает порт
netstat -ano | findstr :5432  # или :8080, :5173

# Убейте процесс
taskkill /PID <PID> /F
```

---

**Status:** ✅ **Готово к разработке!**
